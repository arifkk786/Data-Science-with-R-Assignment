# atomic vector
vector()
vector("character", length=5)
a=c("as","fgh")
typeof(a)
#as.integer to convert numeric to integer
#length(),class(),str(),typeof()
series <- 1:100
series
ser1=seq(100)
ser1
#merged.list=c(list1,list2)
